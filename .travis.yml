language: php

php:
  - stable

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --no-interaction
  - npm insstall 

script:
  - php artisan key:generate
  - vendor/bin/phpunit